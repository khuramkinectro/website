angular.module("userApp", ["appRoutes", "userControllers"]).config(function() {
  // this userApp is use in html page and use with any tag <body > to tell us we use this angular application
  // first argumant app name 2nd depenindiences
  //u wnat to inject routes animation coockies work etc
  // userController is also dependencies use for userCtrl
});
